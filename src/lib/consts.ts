/** Longest edge used for the interactive preview pipeline. Export always runs full-size. */
export const PREVIEW_MAX = 1600;

/** Source cap, carried over from the Python app's constrain_image_size (4096*4096 total pixels). */
export const MAX_PIXELS = 4096 * 4096;
